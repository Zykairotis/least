# Least workers.dev relay

Stable ChatGPT hostname without a bought domain. Deploy once, then start Least with `--tunnel workers-relay`.

```bash
npx wrangler login
least relay-deploy
least start --tunnel workers-relay
```
